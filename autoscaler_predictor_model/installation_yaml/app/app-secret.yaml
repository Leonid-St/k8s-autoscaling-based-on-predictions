# apiVersion: v1
# kind: Secret
# metadata:
#   name: app-secret
# type: Opaque
# data:
#   POSTGRES_DB_NAME: <base64_encoded_db_name> # echo -n 'postgres' | base64
#   POSTGRES_USER: <base64_encoded_db_user> # echo -n 'postgres' | base64
#   POSTGRES_PASSWORD: <base64_encoded_db_password> # echo -n 'ff59-1b1e-4c87-b22' | base64
#   INFLUXDB_TOKEN: <base64_encoded_influxdb_token> # echo -n 'your-token' | base64
